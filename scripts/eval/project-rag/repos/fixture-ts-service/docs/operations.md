# Operations

This fixture repo is a small TypeScript service used by the Project RAG eval suite.

Operational notes:

- Keep the repo structure stable so fixture manifests stay aligned with disk.
- Treat this file as the canonical docs path referenced by the fixture inventory.
